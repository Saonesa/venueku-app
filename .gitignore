# .gitignore for venue-app (monorepo structure)

# --- General Ignores (App-wide) ---
.DS_Store             # macOS system files
*.log                 # Log files
npm-debug.log* # npm debug logs
yarn-debug.log* # yarn debug logs
yarn-error.log* # yarn error logs
.idea/                # JetBrains IDEs folder
.vscode/              # VS Code settings folder
*.sublime-project     # Sublime Text project files
*.sublime-workspace   # Sublime Text workspace files
*.sql                 # Database dumps
*.sql.gz
*.zip
*.tar.gz
*.rar
dump.rdb


# --- Laravel Backend (venue-be) Specific Ignores ---
/venue-be/.env                        # Environment variables (sensitive)
/venue-be/.env.*.local                # Local environment variables
/venue-be/vendor/                     # Composer dependencies
/venue-be/node_modules/               # NPM dependencies (if backend runs its own npm)
/venue-be/public/storage              # The symlink created by storage:link
/venue-be/storage/                    # Laravel's storage folder (except .gitignore)
!/venue-be/storage/.gitignore         # Keep the .gitignore inside storage to track structure
/venue-be/bootstrap/cache/            # Laravel's bootstrap cache
/venue-be/public/build                # Vite's build output (if built into Laravel's public)
/venue-be/.phpunit.result.cache       # PHPUnit cache
/venue-be/Homestead.json              # Homestead specific config
/venue-be/Homestead.yaml              # Homestead specific config
/venue-be/local/                      # Laravel's default local environment folder
/venue-be/phpcs.xml                   # PHP_CodeSniffer config
/venue-be/phpmd.xml                   # PHP Mess Detector config
/venue-be/vendor-bin/                 # php-cs-fixer
/venue-be/storage/*.key               # Encryption keys


# --- React Frontend (venue-fe) Specific Ignores ---
/venue-fe/.env                        # Frontend environment variables (sensitive)
/venue-fe/.env.*.local                # Local frontend environment variables
/venue-fe/node_modules/               # NPM dependencies
/venue-fe/dist/                       # Vite's build output folder
/venue-fe/build/                      # Generic build output folder (if applicable)
/venue-fe/.vite/                      # Vite's internal cache folder